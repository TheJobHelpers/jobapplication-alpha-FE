import { NewClientWizard } from "@/components/admin/new-client-wizard";

// New client onboarding wizard. Static "new" segment resolves before [id].
export default function NewClientPage() {
  return <NewClientWizard />;
}
