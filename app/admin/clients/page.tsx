import { ComingSoon } from "@/components/admin/coming-soon";

export default function ClientsPage() {
  return (
    <ComingSoon
      area="Clients"
      title="Client roster"
      blurb="The roster (with owner column) and the New Client intake flow live here. Each client opens the Client Workspace: This Week shortlist, embedded search, history, and profile."
    />
  );
}
