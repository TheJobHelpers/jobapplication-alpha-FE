import { ClientsRoster } from "@/components/admin/clients-roster";

// Clients roster (client-side so UI-created clients + stage overrides show).
export default function ClientsPage() {
  return <ClientsRoster />;
}
